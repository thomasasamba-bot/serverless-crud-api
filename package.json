{
  "name": "serverless-crud-app",
  "version": "1.0.0",
  "description": "Serverless CRUD API with AWS SAM, Lambda, DynamoDB",
  "main": "index.js",
  "scripts": {
    "start": "sam local start-api",
    "build": "sam build",
    "deploy": "sam deploy"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1042.0",
    "@aws-sdk/lib-dynamodb": "^3.1042.0",
    "bcryptjs": "^2.4.3",
    "dotenv": "^17.4.2",
    "jsonwebtoken": "^9.0.2",
    "uuid": "^11.1.1"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
